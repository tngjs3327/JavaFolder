package Weeks2_2_1.test2;

import Weeks2_2_1.test.Person;

public class Test1 {
  Person p = new Person();
  // 같은 package에 없기 때문에 사용못함, 사용할려면 import해야 된다

  // p.name = "김길동";
  // 접근 불가, default 로 멤버변수가 되어있기 때문에 같은 package가 아니면 접근 불가

}
